{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "dmarc-service.serviceAccountName" . }}
  labels:
    {{- include "dmarc-service.labels" . | nindent 4 }}
{{- end }}
